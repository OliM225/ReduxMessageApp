import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    content: {
        padding: 10,
        borderRadius: 6,
        marginBottom: 5,
        color: "black"
    },
    text: {
        padding: 10,
        color: "red"
    }
})

export default styles;